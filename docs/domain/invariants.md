# Domain Invariants

- The persistent instruction must stay short, behavioral, and repo-agnostic
- Project truth must live inside each target repository, not inside one giant global prompt
- The skill must prefer targeted updates over wholesale doc rewrites
- The installation flow must stay idempotent and safe for repeated execution

